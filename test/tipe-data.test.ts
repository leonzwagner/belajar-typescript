describe("Data type", function () {
  it("must be declare", function () {
    const name: string = "Lalu Akhadi Rizki";
    const balance: number = 10000;
    const isVip: boolean = false;

    console.info(name);
    console.info(balance);
    console.info(isVip);
  });
});
